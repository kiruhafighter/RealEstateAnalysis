@page
@model RealEstateAnalysis.Client.Pages.RegistrationModel
@{
    ViewData["Title"] = "Register";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-header text-center">
                    <h2>Register</h2>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                    {
                    <div class="alert alert-danger">
                        @Model.ErrorMessage
                    </div>
                    }

                    <form method="post" class="form">
                        <div class="form-group mb-4">
                            <label for="Email">Email:</label>
                            <input type="email" class="form-control form-control-lg" id="Email" name="AddUserDto.Email" value="@Model.AddUserDto.Email" required pattern="^[\w\.-]+@@[a-zA-Z\d\.-]+\.[a-zA-Z]{2,4}$" />
                            <span asp-validation-for="AddUserDto.Email" class="text-danger"></span>
                        </div>
                        <div class="form-group mb-4">
                            <label for="FirstName">First Name:</label>
                            <input type="text" class="form-control form-control-lg" id="FirstName" name="AddUserDto.FirstName" value="@Model.AddUserDto.FirstName" required />
                            <span asp-validation-for="AddUserDto.FirstName" class="text-danger"></span>
                        </div>
                        <div class="form-group mb-4">
                            <label for="LastName">Last Name:</label>
                            <input type="text" class="form-control form-control-lg" id="LastName" name="AddUserDto.LastName" value="@Model.AddUserDto.LastName" required />
                            <span asp-validation-for="AddUserDto.LastName" class="text-danger"></span>
                        </div>
                        <div class="form-group mb-4">
                            <label for="Password">Password:</label>
                            <input type="password" class="form-control form-control-lg" id="Password" name="AddUserDto.Password" value="@Model.AddUserDto.Password" required />
                            <span asp-validation-for="AddUserDto.Password" class="text-danger"></span>
                        </div>
                        <div class="form-group mb-4">
                            <label for="ConfirmPassword">Confirm Password:</label>
                            <input type="password" class="form-control form-control-lg" id="ConfirmPassword" name="AddUserDto.ConfirmPassword" value="@Model.AddUserDto.ConfirmPassword" required />
                            <span asp-validation-for="AddUserDto.ConfirmPassword" class="text-danger"></span>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-lg">Register</button>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-center">
                    <small class="text-muted">Already have an account? <a asp-page="/Login">Login here</a>.</small>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

<style>
    body {
        background-color: #f8f9fa;
    }

    .container {
        margin-top: 50px;
    }

    .form-container {
        background: white;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .form-container h1 {
        text-align: center;
        margin-bottom: 30px;
    }

    .form-container label {
        font-weight: bold;
        margin-bottom: 10px;
    }

    .form-control {
        font-size: 1.2em;
    }

    .form-group {
        margin-bottom: 20px;
    }

    .btn {
        background-color: #007bff;
        color: white;
        border: none;
        padding: 10px 20px;
        font-size: 18px;
        cursor: pointer;
    }

    .btn:hover {
        background-color: #0056b3;
    }

    .alert {
        margin-top: 20px;
    }
</style>